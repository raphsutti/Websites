function go(name, age) {
	if (age < 20) {
		return name+ '!';
	} else {
		return name
	}
}

alert( go('Raph', 19) );


function add(first, second) {
	return first+second;
}

alert( "1+2 is " + add(1,2));